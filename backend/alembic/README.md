Alembic migration environment for NeuroFlow cloud backend.

Common commands:

```bash
cd backend
uv run alembic upgrade head
uv run alembic current
```

Current revisions:

- `20260323_01_stage3_schema_foundation.py`
- `20260323_02_sync_push_receipts.py`
