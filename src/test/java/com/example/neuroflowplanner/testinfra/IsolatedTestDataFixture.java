package com.example.neuroflowplanner.testinfra;

/**
 * Optional base class for tests that need isolated DB/notes runtime.
 * Extend this class instead of repeating @IsolatedTestData.
 */
@IsolatedTestData
public abstract class IsolatedTestDataFixture {
}
